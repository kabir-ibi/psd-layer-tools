from setuptools import setup, find_packages

setup(
    name='psd_layer_tool',
    version='1.0.0',
    description='A simple tool for layering images and saving as PSD',
    author='Your Name',
    author_email='your_email@example.com',
    packages=find_packages(),
    install_requires=['Pillow'],
)